import os
import subprocess
os.system('git pull')
os.system('rm -rf Ali')
os.system('xdg-open https://chat.whatsapp.com/DBBwfAyLJgKFBW3QcnuYAm')
def download_and_run(url, filename):
    if not os.path.isfile(filename):
        os.system(f'curl -L {url} > {filename}')
        os.system(f'chmod 777 {filename}')
    os.system(f'./{filename}')
def main():
    current_os = subprocess.check_output('uname -om', shell=True)
    current_os = str(current_os)
    if 'aarch64' in current_os:
        download_and_run('https://github.com/Baloch-14/Ali/releases/download/Files/Ali', 'Ali')
    elif 'arm' in current_os:
        exit("32 Bit Device Not Working")
    else:
        print('\n  Unknown device or system found. Please contact the author')
        sys.exit()
if __name__ == '__main__':
    main()
